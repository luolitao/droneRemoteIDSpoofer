# 2026-05-30

## GB 42590 国标代码核对 + 完整测试
- 全面审查了 GB 42590 国标相关代码，核心传输层和消息编码层均正确
- **gb_swarm.json**: 补全了 operator_id、operator_altitude、start_location、pilot_location 字段
- **scenarios/gb_full_test.json**: 新建完整消息格式测试场景，覆盖 6 架无人机：static/random/waypoints 三种模式，包括空 operator_id、中文 UTF-8 ID、超长 ID 截断、负高度、零值边界等
- **verify_gb_messages.py**: 新建 156 项测试全部通过，覆盖：
  - Test 1: Basic ID (13 tests) — 序列号、ID Type、UA Type、截断、CAA 格式、round-trip
  - Test 2: Location (26 tests) — 经纬度、高度、速度、方向、时间戳、边界值、round-trip
  - Test 3: Self ID (10 tests) — 描述文本、截断、默认值、round-trip
  - Test 4: System (13 tests) — 飞手位置(经纬度+高度)、区域、classification、round-trip
  - Test 5: Operator ID (12 tests) — CAA ID、超长截断、空字符串、中文 UTF-8、round-trip
  - Test 6: GB Message Pack 完整组装 (18 tests) — 129 字节、counter、5 条消息逐条验证
  - Test 7: Counter 递增机制 (6 tests) — 递增 + 溢出回绕
  - Test 8: 协议版本区分 (4 tests) — Proto=1 vs Proto=2
  - Test 9: 场景配置文件验证 (32 tests) — gb_single/gb_swarm/gb_full_test 三个 JSON
  - Test 10: GB Beacon 帧构造 (12 tests) — 802.11 Header、Beacon Body、IE 221、OUI、Capability

## GB 42590 + ASTM F3411-22a 双标准核对
- 逐项比对了 Remote ID国标要求.docx 和 ASTM F3411-22a.docx 与代码实现
- **传输层 (gb.py)**: 完全正确 — Wi-Fi Beacon (Type 0/Subtype 8)、IE 221、OUI FA-0B-BC、OUI Type 0x0D、Counter 0-255、信道 6/2437MHz
- **消息编码 (messages.py)**: 整体正确，发现 4 个低严重度偏差：
  1. Protocol Version = 1 而非 ASTM 要求的 0x2（为兼容 opendroneid-core-c，业界事实标准）
  2. Location 消息 bytes 19-20 Accuracy 字段未设置（默认 0 合法）
  3. System 消息 bytes 20-23 Timestamp 字段未设置（默认 0 合法）
  4. Operator ID 消息 byte 1 注释写"Reserved"实际应为 Operator ID Type（代码行为正确）
- 所有核心字段（经纬度 int32×10⁻⁷、高度 (val+1000)/0.5、速度 ×0.25/0.5、方向 EW 位+0-179°）均正确

## gb_swarm.json 更新为越秀山场景
- 定位坐标改为越秀山（广州）：23.1403°N, 113.2725°E
- 5 架无人机：3 架 random + 2 架 waypoints
- 所有航点/起点/飞手位置距中心 ≤ 137m（远在 1km 限制内）
- 所有高度 ≤ 120m
- 所有 serial 和 operator_id 均含"测试"字样（如 `GB_测试_YueXiu_01`、`GB-OP-测试-YX-01`）

## 全部 16 个场景文件统一为越秀山定位 + 英文"test"ID
- 所有场景 `global.location` 统一为 `[23.1403, 113.2725]`（越秀山）
- 所有坐标（start_location、pilot_location、waypoints）距中心 ≤ 1km
- 所有高度 ≤ 120m（gb_full_test.json 中原本 200m/500m 的已改为 ≤120m）
- 所有 serial 和 operator_id 含英文 "TEST" 字样
- 更新的文件（15个）：gb_single, gb_full_test, gb_swarm（中文改英文）, airport_incursion, ble_single, ble_swarm, ble_stress_test, dual_transport, flight_path, nan_ble_dual, nan_single, nan_swarm, single_random, stress_test, swarm_random, timed_appearance
- 验证脚本确认：16 个场景文件全部通过（位置、高度、ID 三项检查）
